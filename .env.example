# API URL (include /api)
VITE_API_URL=http://localhost:5000/api

# Testing on phone/tablet on same Wi‑Fi: use your PC's LAN IP instead of localhost, e.g.
# VITE_API_URL=http://192.168.1.10:5000/api
# Or keep localhost — the app auto-uses the phone's host when opened via http://192.168.x.x:5173
